# Skill: 启动收敛

## 适用场景

当任务涉及以下内容时使用：

- 新增或删除入口
- 统一启动命令
- 收敛多个 `main` 方法
- 修正终端、IDE、打包启动不一致

## 操作原则

- 优先保留统一入口
- 避免新增新的孤立入口
- 任何运行方式变化都必须更新 `STATUS.md`、`ARCHITECTURE.md`、`modules/runtime.md`
- 发布包里的包装命令要兼容 edition 差异，不能假设所有组件都存在
