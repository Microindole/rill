# 权限与数据库边界

## 身份模型

- `GUEST`：游客，仅默认库 `default`
- 普通用户：可访问已存在数据库，默认库为个人库（若已分配）
- `ADMIN`：管理能力 + 全库访问

## 关键规则

1. 游客只能在 `default` 范围执行
2. 非管理员不能执行 `create/drop database`
3. 管理员可进行用户数据库分配与回收

## 说明

Web 侧权限是“应用层边界”，并不替代内核自身的语义和执行校验。  
建议在演示环境之外配合更严格的鉴权策略与审计日志。

